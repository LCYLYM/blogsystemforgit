name: Build & Deploy Gmeek to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:
  issues:
    types: [opened, edited, labeled, unlabeled, reopened]

permissions:
  contents: read
  issues: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PAGES_TOKEN_EFFECTIVE: ${{ github.token }}
      PAGES_ENABLEMENT: "false"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate site
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          if [ -z "${ISSUE_NUMBER}" ]; then
            python Gmeek.py "${GH_TOKEN}" "${REPO}" --issue_number 0
          else
            python Gmeek.py "${GH_TOKEN}" "${REPO}" --issue_number "${ISSUE_NUMBER}"
          fi

      - name: Resolve Pages token/enablement
        env:
          REPO: ${{ github.repository }}
          PAGES_TOKEN: ${{ secrets.PAGES_TOKEN }}
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -n "${PAGES_TOKEN}" ]; then
            echo "PAGES_TOKEN_EFFECTIVE=${PAGES_TOKEN}" >> "$GITHUB_ENV"
            echo "PAGES_ENABLEMENT=true" >> "$GITHUB_ENV"
            exit 0
          fi

          code=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://api.github.com/repos/${REPO}/pages")
          if [ "${code}" = "404" ]; then
            echo "::error::GitHub Pages 还没启用：请到 Settings > Pages > Build and deployment > Source 选择 GitHub Actions。"
            echo "::error::或者创建一个具备 Pages+Administration 写权限的 PAT，保存为仓库 Secrets: PAGES_TOKEN，然后重跑。"
            exit 1
          fi

          echo "PAGES_TOKEN_EFFECTIVE=${GH_TOKEN}" >> "$GITHUB_ENV"
          echo "PAGES_ENABLEMENT=false" >> "$GITHUB_ENV"

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          token: ${{ env.PAGES_TOKEN_EFFECTIVE }}
          enablement: ${{ env.PAGES_ENABLEMENT }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "docs"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
